def main():
    # input in n
    n = int(input)
    
    maxNum = int(input)
    # maxNum is known Max number.
    line = 2
    while line <= n:
        v > int(input)
        if v > Maxnum:
            maxNum = v
        line = line + 1
    print(maxNum)

main()
